---
interface Props {
  title: string;
  description?: string;
  emoji?: string;
  color?: string;
}
const { title, description = "Lerne Geheimcodes und Chiffren — für Kinder von 8 bis 12!", emoji = "🔐", color = "blue" } = Astro.props;
const pages = [
  { href: "/", label: "Start", emoji: "🏠" },
  { href: "/caesar", label: "Caesar", emoji: "🏛️" },
  { href: "/morse", label: "Morse", emoji: "📡" },
  { href: "/zahlen", label: "Zahlen", emoji: "🔢" },
  { href: "/spiegel", label: "Spiegel", emoji: "🪞" },
  { href: "/pigpen", label: "Pigpen", emoji: "🏗️" },
  { href: "/skytale", label: "Skytale", emoji: "📜" },
  { href: "/emoji", label: "Emoji", emoji: "😎" },
];
const currentPath = Astro.url.pathname;
---
<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
  <meta name="description" content={description} />
  <meta name="apple-mobile-web-app-capable" content="yes" />
  <meta name="apple-mobile-web-app-status-bar-style" content="default" />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <title>{title} — Codes4Children</title>
</head>
<body class="min-h-screen flex flex-col">
  <!-- Navigation -->
  <nav class="sticky top-0 z-50 bg-white/80 backdrop-blur-xl border-b border-gray-200/60">
    <div class="max-w-5xl mx-auto px-6">
      <div class="flex items-center justify-between h-14">
        <a href="/" class="text-lg font-bold text-gray-900 hover:text-apple-blue transition-colors">
          🔐 Codes4Children
        </a>
        <button id="menu-toggle" class="md:hidden w-10 h-10 flex items-center justify-center rounded-xl hover:bg-gray-100 transition-colors" aria-label="Menü">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
          </svg>
        </button>
      </div>
    </div>
    <!-- Nav items -->
    <div id="nav-menu" class="hidden md:block border-t border-gray-100 md:border-0">
      <div class="max-w-5xl mx-auto px-6 py-3 md:py-0 md:-mt-1">
        <div class="flex flex-col md:flex-row md:items-center gap-1 md:gap-0 md:justify-center">
          {pages.map(p => (
            <a href={p.href}
               class:list={[
                 "px-4 py-2.5 md:py-1.5 rounded-xl text-sm font-medium transition-all",
                 currentPath === p.href
                   ? "bg-apple-blue/10 text-apple-blue"
                   : "text-gray-600 hover:text-gray-900 hover:bg-gray-50"
               ]}>
              <span class="mr-1.5">{p.emoji}</span>{p.label}
            </a>
          ))}
        </div>
      </div>
    </div>
  </nav>

  <main class="flex-1">
    <slot />
  </main>

  <!-- Footer -->
  <footer class="border-t border-gray-100 mt-24">
    <div class="max-w-5xl mx-auto px-6 py-10 text-center">
      <p class="text-sm font-medium text-gray-500">
        Ein Projekt der <a href="https://manyos.it" target="_blank" class="text-gray-700 hover:text-apple-blue transition-colors">manyos technology GmbH</a>
      </p>
      <div class="flex items-center justify-center gap-4 mt-3 text-xs text-gray-400">
        <a href="https://manyos.it/impressum" target="_blank" class="hover:text-gray-600 transition-colors">Impressum</a>
        <span>·</span>
        <a href="https://manyos.it/datenschutz" target="_blank" class="hover:text-gray-600 transition-colors">Datenschutz</a>
      </div>
    </div>
  </footer>

  <script>
    const toggle = document.getElementById('menu-toggle');
    const menu = document.getElementById('nav-menu');
    toggle?.addEventListener('click', () => {
      menu?.classList.toggle('hidden');
    });
  </script>
</body>
</html>
